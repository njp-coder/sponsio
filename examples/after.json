{
  "toolpact": 1,
  "url": "https://shop.example",
  "capturedAt": "2026-09-05T10:00:00.000Z",
  "tools": [
    { "name": "search_products", "description": "Find items in the store", "kind": "imperative",
      "annotations": { "readOnly": true },
      "inputSchema": { "type": "object",
        "properties": { "query": { "type": "string" },
          "pillar": { "type": "string", "enum": ["home", "garden"] },
          "locale": { "type": "string" } },
        "required": ["query", "locale"] } },
    { "name": "add_to_cart", "description": "Add a product to the cart", "kind": "imperative",
      "inputSchema": { "type": "object",
        "properties": { "product_id": { "type": "string" }, "quantity": { "type": "number", "maximum": 5 } },
        "required": ["product_id"] } },
    { "name": "checkout", "description": "Begin checkout", "kind": "declarative",
      "annotations": {},
      "inputSchema": { "type": "object", "properties": {} } },
    { "name": "track_order", "description": "Look up order status", "kind": "imperative" }
  ]
}
